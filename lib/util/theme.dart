import 'package:cht/util/colorConstants.dart';
import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData basicTheme = ThemeData(
    accentColor: ColorLight.themeColor,
    primaryColor: ColorLight.white,
    brightness: Brightness.dark,
    scaffoldBackgroundColor: ColorLight.themeColor,
    // textTheme: TextTheme()
  );
}
